# @package _global_
defaults:
  - override /dataset: utsd
  - override /model: small

batch_size: 16384
num_gradient_accumulation_steps: 2

epochs: 10

train_mixed_precision: true

save_examples: false
wandb_project: "utsd_dropout"

num_devices: 16

model:
  partial_layer_dropout_prob: 0.0

optimizer:
  learning_rate: 0.00005
